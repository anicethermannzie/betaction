-r requirements.txt
pytest==8.2.0
pytest-asyncio==0.23.6
pytest-cov==5.0.0
httpx==0.27.0
# Pinned: CI installed ruff unpinned, so a new release could fail the build on
# rules the code was never written against.
ruff==0.6.9
